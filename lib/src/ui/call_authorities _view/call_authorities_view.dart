import 'package:flutter/material.dart';

class CallAuthorities extends StatelessWidget {
  const CallAuthorities({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
